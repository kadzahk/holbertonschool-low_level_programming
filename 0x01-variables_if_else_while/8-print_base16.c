#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
int num;
char al;
for (num = '0'; num <= '9'; num++)
putchar(num);
for (al = 'a'; al <= 'f'; all++)
putchar(all);
putchar('\n');
return (0);
}
