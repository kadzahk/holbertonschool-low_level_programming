#include "main.h"
/**
 * _abs - function
 * @a: variable of the function
 * Return: dependent of the function
 */
int _abs(int a)
{
if (a > 0)
{
return (a);
}
else if (a < 0)
{
return (-a);
}
return (0);
		}
